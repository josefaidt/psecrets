{
  "name": "psecrets-dev",
  "type": "module",
  "private": true,
  "version": "0.1.0",
  "bin": "./bin/cli.js",
  "scripts": {
    "copy-cli": "test -f ../psecrets-cli/build/cli.js && mkdir -p bin && cp ../psecrets-cli/build/cli.js bin/",
    "prepare": "vite-node scripts/sync-version.ts && pnpm copy-cli"
  },
  "dependencies": {
    "psecrets-cli": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^18.15.3",
    "typescript": "^5.0.2",
    "vite-node": "^0.29.3"
  }
}